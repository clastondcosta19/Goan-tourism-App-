// historical_places.dart
import 'package:flutter/material.dart';

class HistoricalSitesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Historical Sites'),
      ),
      body: Center(
        child: Text('Historical Sites Page'),
      ),
    );
  }
}
